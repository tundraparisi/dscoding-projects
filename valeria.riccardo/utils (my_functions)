for question in easy_questions:
    print(np.random.choice(easy_questions, replace=False)) #shuffling option to add
    #ask the user for a guess
    reply = input("Enter your answer (A, B, C, D): ").upper() #it works both with small and capital letters
    your_answers.append(reply) #adding the guess into the list of the answers in order to compute the score later
    if reply == easy_right_answers:
        score += 1
        print("Very good!")
    else:
        print("Not really good")
    question_num += 1   
    #we continue with the next question. 
