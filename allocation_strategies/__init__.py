# qui posso mettere hello world per farlo poi eseguire in un'altra directory
# lui dentro forces ha una classe con dentro una funzione, poi va dentro __init__
# from .forces import Forces
# o anche from physics import Forces (inteso come Forces il file)

from .random_allocation import Hello

Hello()